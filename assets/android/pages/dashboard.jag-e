<%
var log = new Log(),
        caramel = require('caramel').caramel,
        site = require('/modules/site.js'),
        store = require('/modules/store.js'),
        tag = require('/modules/tag.js');

caramel.render({
    'title': 'Store | Android Apps',
    'navigation': site.navigation
});
%>